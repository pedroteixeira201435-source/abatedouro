import { Product, Customer, Sale } from './types';

/**
 * Seed data for a brand-new install. Empty in production — each business enters its own
 * products, customers and sales. (Kept as named exports so the data layer has a single source.)
 */
export const DUMMY_PRODUCTS: Product[] = [];
export const DUMMY_CUSTOMERS: Customer[] = [];
export const DUMMY_SALES: Sale[] = [];
